<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kuyodynamics.commcaresurveymanager">

    <application
        android:name="com.kuyodynamics.commcaresurveymanager.core.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommCareSurveyManager">
        <activity
            android:name="com.kuyodynamics.commcaresurveymanager.core.MainActivity"
            android:theme="@style/Theme.CommCareModuleManager.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>